"use client";

import AccountDeletionPage from "@/components/operations/account/AccountDeletionPage";

export default function AccountSettingsPage() {
  return (
    <main className="w-full h-full overflow-auto hide-scrollbar">
      <AccountDeletionPage />
    </main>
  );
}
