"use client";

import ClientPreferencePage from "@/components/operations/clients/preferences/ClientPreferencePage";

export default function ClientPage() {
  return (
    <main className="w-full h-full overflow-auto hide-scrollbar">
      <ClientPreferencePage />
    </main>
  );
}
